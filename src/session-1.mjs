/**
 * It's all about primitive data types and the use of very simple things like numbers and lists.
 */

const array = ['asdf', 'abc', 'xyz'];

const something = array.map((value) => {
  return (value += `${val} `);
});

console.log(something);
