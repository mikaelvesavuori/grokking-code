# node-minimalist-starter

A minimalist starting point for TS/MJS projects.

## Commands

Install with `npm install`.

Run tests with `npm test` to run all tests once, or `npm run test:watch` to run them continuously.
